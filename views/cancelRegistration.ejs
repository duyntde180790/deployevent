<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cancel Registration</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
      body {
        min-height: 100vh;
        background: linear-gradient(135deg, #a4508b 0%, #5f0a87 100%);
      }
      .cancel-card {
        background: rgba(255,255,255,0.95);
        border-radius: 1.5rem;
        box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.18);
        padding: 2rem 2rem 1.5rem 2rem;
        margin-top: 2rem;
        border: 1px solid rgba(255,255,255,0.18);
        max-width: 500px;
        margin-left: auto;
        margin-right: auto;
      }
      .cancel-title {
        color: #5f0a87;
        font-weight: 700;
        letter-spacing: 1px;
        text-align: center;
        margin-bottom: 1.5rem;
      }
      .btn-gradient {
        background: linear-gradient(90deg, #a4508b 0%, #5f0a87 100%);
        border: none;
        color: #fff;
        font-weight: 600;
        transition: background 0.3s;
      }
      .btn-gradient:hover {
        background: linear-gradient(90deg, #5f0a87 0%, #a4508b 100%);
        color: #fff;
      }
      .btn-cancel {
        background: #e74c3c;
        border: none;
        color: #fff;
        font-weight: 600;
        transition: background 0.3s;
      }
      .btn-cancel:hover {
        background: #c0392b;
        color: #fff;
      }
    </style>
</head>
<body>
    <div class="cancel-card">
        <h2 class="cancel-title">Cancel Registration</h2>
        <form method="POST" action="/api/registrations/<%= registrationId %>?_method=DELETE">
            <p>Are you sure you want to cancel your registration for event: <strong><%= eventName %></strong>?</p>
            <button type="submit" class="btn btn-cancel">Yes, Cancel</button>
            <a href="/" class="btn btn-gradient">No, Go Back</a>
        </form>
    </div>
</body>
</html> 