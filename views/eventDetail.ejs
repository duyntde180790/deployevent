<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chi tiết sự kiện</title>
    <%- include('partials/style.ejs') %>
</head>
<body>
    <div class="header">Hệ thống sự kiện - Chi tiết sự kiện</div>
    <%- include('partials/studentNav') %>
    <div class="container">
        <h2 style="text-align:center; margin-bottom: 24px;">Chi tiết sự kiện</h2>
        <% if (typeof message !== 'undefined') { %>
            <div class="alert"><%= message %></div>
        <% } %>
        <% if (event) { %>
            <div>
                <h3><%= event.name %></h3>
                <p><strong>Mô tả:</strong> <%= event.description %></p>
                <p><strong>Ngày bắt đầu:</strong> <%= event.date ? new Date(event.date).toLocaleString() : '' %></p>
                <p><strong>Ngày kết thúc:</strong> <%= event.endDate ? new Date(event.endDate).toLocaleString() : '' %></p>
                <p><strong>Địa điểm:</strong> <%= event.location %></p>
                <p><strong>Số lượng tối đa:</strong> <%= event.maxCapacity %></p>
                <p><strong>Đã đăng ký:</strong> <%= registered %></p>
            </div>
        <% } else { %>
            <div class="alert">Không tìm thấy sự kiện</div>
        <% } %>
        <a href="/register-event" class="btn" style="margin-top:16px;">Quay lại danh sách sự kiện</a>
    </div>
</body>
</html> 