<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tạo sự kiện mới</title>
    <%- include('partials/style.ejs') %>
</head>
<body>
    <div class="header">Hệ thống sự kiện - Quản trị viên</div>
    <%- include('partials/adminNav') %>
    <div class="container">
        <h2 style="text-align:center; margin-bottom: 24px;">Tạo sự kiện mới</h2>
        <% if (typeof message !== 'undefined') { %>
            <div class="alert"><%= message %></div>
        <% } %>
        <form method="POST" action="/admin/create-event">
            <label for="name">Tên sự kiện</label>
            <input type="text" id="name" name="name" required>
            <label for="description">Mô tả</label>
            <textarea id="description" name="description"></textarea>
            <label for="date">Ngày bắt đầu</label>
            <input type="datetime-local" id="date" name="date" required>
            <label for="endDate">Ngày kết thúc</label>
            <input type="datetime-local" id="endDate" name="endDate">
            <label for="location">Địa điểm</label>
            <input type="text" id="location" name="location" required>
            <label for="maxCapacity">Số lượng tối đa</label>
            <input type="number" id="maxCapacity" name="maxCapacity" min="1" required>
            <button type="submit" class="btn" style="width:100%;margin-top:8px;">Tạo sự kiện</button>
        </form>
    </div>
</body>
</html>
